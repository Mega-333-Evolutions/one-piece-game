1. **Remove ptb from `requirements.txt` and install `telethon`**. Replace `python-telegram-bot` with `telethon`.
2. **Rewrite `main.py`**. The entire bot initialisation needs to change from ptb's `Application.builder()` to telethon's `TelegramClient`. Handlers must be rewritten from `CommandHandler`, `MessageHandler`, `CallbackQueryHandler`, etc. to telethon's `events.NewMessage` and `events.CallbackQuery`.
3. **Refactor core update handlers**. `src/chat/manage_message.py` receives Telegram updates. The `update` and `context` objects are entirely ptb-specific. They will need to be changed to `event` (telethon event) and the bot client itself. All usages of `update.effective_user`, `update.effective_chat`, `update.callback_query`, `update.message.text` etc. must be changed to `event.sender_id`, `event.chat_id`, `event.text`, etc.
4. **Refactor context handling**. PTB uses `context.user_data`, `context.bot_data` and `context.chat_data`. Telethon does not have built-in context data like this. We will need to implement a simple dictionary-based context store or rely on the existing database for long-term state, and maybe a simple memory structure for short-term state.
5. **Refactor `message_service.py`**. This service handles sending and editing messages. We need to replace all usages of `context.bot.send_message`, `update.effective_message.reply_text`, etc. with `client.send_message` and `event.reply`. Inline keyboards also need to be rewritten to telethon's `Button.inline`.
6. **Refactor all screens**. There are 135 files that import from `telegram` and use PTB concepts. Every single one needs to be updated. The signature of `manage(update, context)` will change to `manage(event, client)`.
7. **Refactor user service and other services**. Getting user photos, chat members, chat administration status all use PTB specific API. These must be migrated to `client(GetFullUserRequest)`, `client.get_permissions()`, etc.
8. **Fix all types**. Change typed arguments from `Update` and `ContextTypes.DEFAULT_TYPE` to telethon types.
9. **Verify**. Use mypy/flake8 and manual testing to ensure there are no lingering PTB imports. Run tests if any exist. Complete pre-commit instructions. Submit.
